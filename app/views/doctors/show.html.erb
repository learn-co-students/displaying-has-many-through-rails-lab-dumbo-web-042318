<h1><%= @doctor.name %></h1>
<p>Patient Count: <%= @doctor.patients.count %></p>

<% @doctor.appointments.each do |appt| %>
  <p><%= appt.nice_time %> with <%= link_to appt.patient.name, patient_path(appt.patient)%></p>
<% end %>
