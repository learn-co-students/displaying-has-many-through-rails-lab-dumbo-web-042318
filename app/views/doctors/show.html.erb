<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>
<ul>
  <% @appointments.each do |app| %>
    <li><%= app.datetime_format %> -- <%= link_to app.patient.name, patient_path(app.patient.id) %></li>
  <% end %>
</ul>
