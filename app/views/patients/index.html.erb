<h1>Check out all these patients!</h1>

<ul>
<% @patients.each do |pat| %>
  <li>
    Name: <%= link_to pat.name, patient_path(pat) %> Number of Appointments: <%= pat.appointments.count %>
  </li><br>
  <% end %>
</ul>
